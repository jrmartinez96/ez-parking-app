import 'package:flutter/material.dart';

const boxShadow = [
  BoxShadow(
    color: Color(0x2A000000),
    offset: Offset(2, 2),
    blurRadius: 6.0,
  )
];

const blackBlur = <BoxShadow>[
  BoxShadow(
    color: Colors.black12,
    offset: Offset(0.0, 1.0),
    blurRadius: 20.0,
  ),
];
